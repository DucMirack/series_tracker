%h1{:align => "center"} Toute les séries
- @series.each do |serie|
  .row
    .col-xs-8.col-xs-offset-2
      .well.well-lg
        -# .serie-profile-image
        -#   = image_tag serie.profile_image.thumb.url
        .serie-title
          = link_to serie.title, serie_path(serie)
        .serie-body
          = truncate(serie.description, length: 100)
        .serie-actions
          = link_to "Modifier la série", edit_serie_path(serie), class: "btn btn-xs btn-primary"
          = link_to "Supprimer la série", serie_path(serie), method: :delete, data: { confirm: "Êtes vous sur ?"}, class: "btn btn-xs btn-danger"
