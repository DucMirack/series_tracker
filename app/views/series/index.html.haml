%h1{:align => "center"} Tout les films
- @series.each do |serie|
  .row
    .col-xs-8.col-xs-offset-2
      .well.well-lg
        .serie-title
          = link_to serie.title, serie_path(serie)
        .serie-body
          = truncate(serie.description, length: 100)
        .serie-actions
          = link_to "Modifier le film", edit_serie_path(serie), class: "btn btn-xs btn-primary"
          = link_to "Supprimer le film", serie_path(serie), method: :delete, |
            data: { confirm: "Êtes vous sur ?"},      |
            class: "btn btn-xs btn-danger"                                         |
